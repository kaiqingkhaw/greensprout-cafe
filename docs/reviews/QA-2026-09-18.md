# Verification - 18 September 2026

## Changes reviewed

- Every HTML document now declares `assets/favicon.svg?v=2`. Admin pages and customer profiles no longer fall back to the XAMPP icon. The versioned URL also avoids the previous icon cache.
- Admin insight/export date parameters tolerate array inputs instead of raising a type error.
- Checkout validates discount codes as bounded text before calculating prices.
- Admin password changes reject all-space passwords. The setup command now validates username format and the password byte limit used at login.
- CSRF token requests respect the caller cancellation signal and otherwise time out after 12 seconds.

## Results

- 106 localhost HTTP checks passed; temporary test accounts, orders, reviews, contact entries and uploaded photo were removed.
- 25 desktop/mobile browser checks passed, including customer and admin login, both profile pages, catalogue search/filter/sort, cart and form validation. No horizontal overflow or browser JavaScript errors were detected.
- Each visited page exposed exactly one expected icon link. The icon responded with HTTP 200 and SVG content type.
- All 14 HTML documents passed the shared-icon check, including the legacy redirect and sign-out page.
- 27 PHP files passed syntax checks. Frontend syntax and behavioural regression checks passed.
- 234 static local navigation, media and stylesheet references resolved; no missing files found.
- Text source files were checked for valid UTF-8 encoding.

## Scope

Server request handling, authentication, role checks, input validation, database writes, rendered output and client behaviours were reviewed alongside the automated checks. Static checks scan the first-party source tree; bundled third-party Font Awesome code was not manually audited. These results are evidence for the tested paths, not a guarantee that every possible input or browser is bug-free.

The previous report retains the complete order-journey and video-control checks: [17 September](QA-2026-09-17.md).

## Repository organisation follow-up

The website now lives in `public/`, shared PHP helpers in `app/`, videos in `public/assets/videos/`, and review notes in `docs/reviews/`. The README records the group-project origin and subsequent personal portfolio revision.

After the moves, all frontend checks, 28 PHP syntax checks, 106 HTTP checks and 25 desktop/mobile page checks passed again. The complete mobile order journey also passed: customer signup/login, customisation, checkout, persistent invoice, profile history, admin status update and customer tracking. Its temporary records were removed.

The read-only `npm run test:structure` suite verifies the project entry point, old-page redirects, private directory restrictions and relocated videos/icon on Apache. It passed all 15 checks. GitHub Actions passed for the original published commit; the organisation update must run separately after publication.
